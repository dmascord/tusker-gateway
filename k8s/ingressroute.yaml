apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: tusker-gateway
  namespace: hermes
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`ai.tusker.net.au`)
    kind: Rule
    services:
    - name: tusker-gateway
      port: 8642
  tls:
    certResolver: letsencrypt
